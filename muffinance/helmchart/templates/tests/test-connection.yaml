apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "muffinance.fullname" . }}-test-connection"
  labels:
    {{- include "muffinance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "muffinance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

