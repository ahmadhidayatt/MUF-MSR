kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ .Chart.Name }}
spec:
  podSelector: 
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
  ingress:
   - {}
  policyTypes:
   - Ingress
