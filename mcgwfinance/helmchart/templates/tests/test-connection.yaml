apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcgwfinance.fullname" . }}-test-connection"
  labels:
    {{- include "mcgwfinance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcgwfinance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
