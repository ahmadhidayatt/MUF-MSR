apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcgwcustomer.fullname" . }}
  labels:
    {{- include "mcgwcustomer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ .Values.targetPort }}
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
  selector:
    {{- include "mcgwcustomer.selectorLabels" . | nindent 4 }}